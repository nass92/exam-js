const average = (array) => {
  for (i = 0; i < array.length; i++) {
    return i / array.legnth

  }
}
console.log(average([1, 1, 1]))

// pas eu le temps de trouver mon erreur je laisse comme meme le travaille fait.